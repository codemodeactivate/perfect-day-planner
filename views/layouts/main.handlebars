<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="description" content="A simple blog built with Node.js, Express, Handlebars, and MongoDB.">
        <meta name="keywords" content="node.js,express,handlebars,mongodb,blog">
        <title>Perfect Day Planner</title>
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <script src="https://cdnjs.cloudflare.com/ajax/libs/zxcvbn/4.4.2/zxcvbn.js"></script>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
        <link rel="stylesheet" href="/css/reset.css">
        <link rel="stylesheet" href="/css/style.css">
    </head>
    <body>
        <div class="container"> <!-- container for bootstrap -->
            <header class="text-center">
                <h1>
                    <a href="/" style="text-decoration:none" class="text-success">Perfect Day Planner</a>
                </h1>
                <nav class="navbar d-flex justify-content-center">
                    {{#if logged_in}}
                        <a href="/dashboard" class="btn btn-primary mx-5">Dashboard</a>
                        <a href="/logout" class="btn btn-primary mx-5">Logout</a>
                    {{else}}
                        <a href="/signup" class="btn btn-primary mx-5">Sign Up</a>
                        <a href="/login" class="btn btn-primary mx-5">Login</a>
                    {{/if}}
                </nav>
            </header>
        <main>
            {{{body}}}
        </main>
        <footer>
            {{{footer}}}
        </footer>
        </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    </body>
</html>
